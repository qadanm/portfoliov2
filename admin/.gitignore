# Build artifacts
dist/
.astro/
node_modules/

# Local wrangler dev state (in-memory KV cache, dev session)
.wrangler/

# Local data — never commit
.env
.env.local
*.local

# Generated PDFs / exports — keep out of git
exports/
*.pdf
