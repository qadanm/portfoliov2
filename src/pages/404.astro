---
import BaseLayout from '@/components/layout/BaseLayout.astro';
---

<BaseLayout
  title="404, Not Found"
  description="Page not found."
  noindex={true}
>
  <div class="nf">

    <div class="container-site nf__inner">
      <div class="nf__sxn">
        <span class="nf__sxn-label">Off the path</span>
        <span class="nf__sxn-rule" aria-hidden="true"></span>
        <span class="nf__sxn-meta">Page not found</span>
      </div>

      <h1 class="nf__title">
        This page <em>doesn't exist</em>.<br />
        Or it moved.
      </h1>

      <p class="nf__lede">
        Either way, pick a thread below.
      </p>

      <div class="nf__links">
        <a href="/" class="nf__link">
          <span class="nf__link-body">
            <span class="nf__link-title">Home</span>
            <span class="nf__link-sub">Start at the beginning.</span>
          </span>
          <span class="nf__link-arrow" aria-hidden="true">→</span>
        </a>
        <a href="/work" class="nf__link">
          <span class="nf__link-body">
            <span class="nf__link-title">Work</span>
            <span class="nf__link-sub">Selected projects and case studies.</span>
          </span>
          <span class="nf__link-arrow" aria-hidden="true">→</span>
        </a>
        <a href="/approach" class="nf__link">
          <span class="nf__link-body">
            <span class="nf__link-title">Approach</span>
            <span class="nf__link-sub">How I design, build, and use AI in production.</span>
          </span>
          <span class="nf__link-arrow" aria-hidden="true">→</span>
        </a>
        <a href="/about" class="nf__link">
          <span class="nf__link-body">
            <span class="nf__link-title">About</span>
            <span class="nf__link-sub">Background and how I think.</span>
          </span>
          <span class="nf__link-arrow" aria-hidden="true">→</span>
        </a>
        <a href="/contact" class="nf__link">
          <span class="nf__link-body">
            <span class="nf__link-title">Contact</span>
            <span class="nf__link-sub">Send a message.</span>
          </span>
          <span class="nf__link-arrow" aria-hidden="true">→</span>
        </a>
      </div>
    </div>
  </div>
</BaseLayout>

<style>
  .nf {
    position: relative;
    padding-top: calc(60px + var(--space-10));
    padding-bottom: var(--space-16);
    min-height: 80vh;
  }

  @media (min-width: 768px) {
    .nf {
      padding-top: calc(64px + var(--space-20));
      padding-bottom: var(--space-24);
    }
  }



  .nf__inner {
    position: relative;
    max-width: 720px;
  }

  /* Numbered head */
  .nf__sxn {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: var(--space-5);
    flex-wrap: wrap;
  }


  .nf__sxn-label {
    font-family: var(--font-body);
    font-size: 13px;
    font-weight: 600;
    color: var(--color-text);
  }

  .nf__sxn-rule {
    flex: 1;
    height: 1px;
    background: linear-gradient(to right, var(--color-border), transparent);
    min-width: 32px;
  }

  .nf__sxn-meta {
    font-family: var(--font-body);
    font-size: 11px;
    color: var(--color-text-subtle);
    white-space: nowrap;
  }

  .nf__title {
    font-family: var(--font-display);
    font-size: clamp(38px, 9vw, 72px);
    font-weight: 800;
    letter-spacing: -0.025em;
    line-height: 1.05;
    color: var(--color-text);
    margin-bottom: var(--space-5);
    max-width: 18ch;
  }

  /* "This page doesn't exist." — the phrase recedes instead of leaning.
     No italic face is loaded, so italic here would be a synthesized oblique;
     with a real weight axis, dropping to 400 inside an 800 headline says the
     same thing and says it better. */
  .nf__title em {
    font-style: normal;
    font-weight: 400;
    color: var(--color-accent);
  }

  .nf__lede {
    font-size: clamp(15px, 1.5vw, 17px);
    line-height: 1.7;
    color: var(--color-text-muted);
    max-width: 52ch;
    margin-bottom: var(--space-10);
  }

  .nf__links {
    display: flex;
    flex-direction: column;
    gap: 0;
    border-top: 1px solid var(--color-border-subtle);
  }

  .nf__link {
    display: grid;
    grid-template-columns: 28px 1fr auto;
    gap: var(--space-3);
    align-items: center;
    padding: var(--space-4) 0;
    min-height: 64px;
    border-bottom: 1px solid var(--color-border-subtle);
    text-decoration: none;
    color: var(--color-text);
    transition: padding-left var(--duration-base) var(--ease-out);
    -webkit-tap-highlight-color: transparent;
  }

  @media (min-width: 600px) {
    .nf__link {
      grid-template-columns: 36px 1fr auto;
      gap: var(--space-4);
      padding: var(--space-5) 0;
    }
  }

  .nf__link:hover {
    padding-left: 8px;
  }


  .nf__link:hover
  .nf__link-body {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .nf__link-title {
    font-family: var(--font-display);
    font-size: 20px;
    font-weight: 600;
    letter-spacing: -0.02em;
    color: var(--color-text);
  }

  @media (min-width: 600px) {
    .nf__link-title {
      font-size: 22px;
    }
  }

  .nf__link-sub {
    font-size: 13px;
    color: var(--color-text-muted);
    line-height: 1.4;
  }

  .nf__link-arrow {
    font-size: 18px;
    color: var(--color-text-subtle);
    transition: color var(--duration-base) var(--ease-out), transform var(--duration-base) var(--ease-out);
  }

  .nf__link:hover .nf__link-arrow {
    color: var(--color-accent);
    transform: translateX(6px);
  }
</style>
