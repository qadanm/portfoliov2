PUBLIC_WEB3FORMS_KEY=your-web3forms-access-key-here

# Plausible Analytics — leave unset (or empty) in dev to skip loading the
# script entirely. In production set this to the bare hostname registered in
# Plausible (e.g. `qadan.co`, no protocol or trailing slash).
PUBLIC_PLAUSIBLE_DOMAIN=
